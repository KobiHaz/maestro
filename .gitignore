# macOS
.DS_Store

# Obsidian
.obsidian/workspace.json
*.sync-conflict*
# Local REST API stores API key + TLS private key — never commit
.obsidian/plugins/obsidian-local-rest-api/data.json

# Cursor
.cursor/.cursoragent
.cursor/*.log

# Local npm (scripts under 06-outputs — reinstall with npm install)
06-outputs/node_modules/
